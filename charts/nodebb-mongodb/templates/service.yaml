apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodebb_mongo.fullname" . }}
  labels:
    {{- include "nodebb_mongo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodebb_mongo.selectorLabels" . | nindent 4 }}
